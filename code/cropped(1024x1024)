# Step 1: YOLOv11s 학습 설정 정의
# - 모델 설정 파일, 하이퍼파라미터(yaml), 데이터셋 경로 설정

# Step 2: 1024x1024로 크롭된 데이터셋 로딩
# - train/val 디렉토리 구조를 지정된 형식에 맞춰 준비

# Step 3: 하이퍼파라미터 설정 및 학습 실행
# - batch size, epoch 수, conf threshold 등 실험적으로 설정

# Step 4: 학습 완료 후 가중치 저장
# - best.pt 또는 last.pt 파일로 저장하여 추론에 사용
